import margin from './margin';
import padding from './padding';
import text from './size';
import color from './colors';

const theme = {
  margin,
  padding,
  text,
  color,
};

export default theme;
